{
  "name": "skill-manager-readme-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Reproducible Remotion source for the Skill Manager README demo",
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=10.0.0"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "fsevents@2.3.3": true
  },
  "scripts": {
    "studio": "remotion studio --no-open",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/storyboard.test.ts",
    "check": "npm run typecheck && npm test",
    "render": "remotion render SkillManagerOverview out/demo-master.mp4 --codec=h264 --crf=18 --pixel-format=yuv420p --muted",
    "social-preview": "remotion still SkillManagerSocialPreview ../.github/assets/social-preview.png",
    "stills": "remotion still SkillManagerOverview out/still-problem.png --frame=45 && remotion still SkillManagerOverview out/still-terminal.png --frame=120 && remotion still SkillManagerOverview out/still-dashboard.png --frame=210 && remotion still SkillManagerOverview out/still-toggle.png --frame=338 && remotion still SkillManagerOverview out/still-sources.png --frame=465 && remotion still SkillManagerOverview out/still-close.png --frame=555",
    "gif": "./scripts/render-gif.sh",
    "verify": "./scripts/verify-output.sh"
  },
  "dependencies": {
    "@remotion/cli": "4.0.509",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "remotion": "4.0.509"
  },
  "devDependencies": {
    "@types/node": "24.10.13",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.3",
    "tsx": "4.23.12",
    "typescript": "5.9.3"
  }
}
